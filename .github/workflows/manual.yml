name: Manual build

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name'
        type: string
        default: 'jyothika'
  


jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: Greetings
        run:  echo "${{ inputs.name }}"
